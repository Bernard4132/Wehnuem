<%= form_with(model: lead, local: true) do |form| %>
  <% if lead.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lead.errors.count, "error") %> prohibited this lead from being saved:</h2>

      <ul>
      <% lead.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :lead_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :lead_email %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :lead_phone %>
  </div>

  <div class="field">
    <%= form.label :timeappoint %>
    <%= form.text_field :timeappoint, id: :lead_timeappoint %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message, id: :lead_message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
